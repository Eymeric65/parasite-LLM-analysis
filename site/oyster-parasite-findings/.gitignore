# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Database files fetched from GitHub Releases at build time.
# In local dev, public/database is a symlink to ../../database/ (tracked).
# If this directory gets replaced by actual files (e.g. by running the CI
# build steps locally), those files must NOT be committed.
public/database/*.jsonl
public/database/*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
